{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "next-app",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "set -a && [ -f ../../../.env ] && . ../../../.env; set +a && cd ../../../ && pnpm --filter @workspace/next-app run dev"],
      "port": 3000,
      "autoPort": true
    }
  ]
}
